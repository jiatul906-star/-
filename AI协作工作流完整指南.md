# AI 协作工作流完整指南

> 本文档解释"为什么这样设计"而非仅仅"设计成什么样"。
> 目标读者：任何想用 AI agent 辅助多人协作开发的人。

---

## 目录

1. [这套工作流解决什么问题](#一这套工作流解决什么问题)
2. [核心机制：宪法优先](#二核心机制宪法优先)
3. [宪法执行机制：CLAUDE.md](#三宪法执行机制claudemd)
4. [翻译层：让非技术人员和程序员能协作](#四翻译层让非技术人员和程序员能协作)
5. [多维决策表：让非技术人员参与技术选型](#五多维决策表让非技术人员参与技术选型)
6. [Spec 作为契约：让非技术人员能验收](#六spec-作为契约让非技术人员能验收)
7. [素材管线：美术和代码的交接](#七素材管线美术和代码的交接)
8. [风险分级与阻塞关系图：知道先修什么](#八风险分级与阻塞关系图知道先修什么)
9. [活文档与 Git 自动同步](#九活文档与-git-自动同步)
10. [流程自更新](#十流程自更新)
11. [完整落地清单：如何在新项目中搭建这套体系](#十一完整落地清单如何在新项目中搭建这套体系)

---

## 一、这套工作流解决什么问题

### 背景

三个人协作开发一个桌面应用：
- **用户**（产品/美术）：负责素材、UI 设计、需求决策
- **程序员**：负责代码、架构、技术决策
- **Claude**：AI agent，辅助双方工作

### 第一次尝试：崩溃

程序员用 AI 快速写出了 v0.1.0——聊天、桌宠、设置三个模块都能跑。看起来不错。然后加入 DIY 功能，代码崩溃。

审查发现三个致命 Bug：
- **AI 对话是假的**：6 条固定文案随机选，完全不调用 API
- **设置完全不持久化**：5 个表单字段未绑定 value/onChange，用户输入即丢弃
- **源码与构建产物脱节**：`src/` 是空壳，全部逻辑在 ASAR 中，无法迭代维护

**根因不是程序员不行，而是 AI 辅助编程的系统性缺陷**：每次 AI 对话独立写一个功能，没有全局约束。三个对话写得都对——拼在一起全错。

### 这套工作流要解决的三个根本问题

| 问题 | 表现 | 根因 |
|------|------|------|
| **信息怎么不丢？** | 用户说"桌宠可爱"→ 程序员不知道要建几个动画状态 | 双方语言不通，需求在传递中衰减 |
| **决策怎么做？** | TTS 方案没人会选；做完的功能用户说不对 | 技术决策是不透明的黑盒；验收没有客观标准 |
| **过程怎么不腐？** | 文档写完两周就过时；流程定好两周就不适用 | 维护文档和流程是额外负担，没人做 |

---

## 二、核心机制：宪法优先

### 是什么

在写任何代码之前，先创建一套"宪法文档"。每次 AI 写代码前**必须先读这些文件**。不是建议，是硬约束。

### 宪法文档清单

```
docs/
├── vision.md           # 产品愿景：一句话 + 目标用户 + 竞品差异
├── features.md         # 功能清单 + 子功能拆解 + 优先级
├── architecture.md     # 技术栈、模块拆分、数据流、窗口架构
├── interfaces.md       # 模块间接口契约：IPC通道、事件总线、数据类型
├── design-system.md    # 所有视觉token：色值/间距/圆角/阴影/动效（精确到像素）
└── ui-spec.md          # 交互逻辑：三级结构、状态转换、边界情况决策
```

### 为什么这样设计

**案例：无宪法时的代码一致性灾难**

v0.1.0 开发时没有 architecture.md 和 interfaces.md。程序员的三次 AI 对话：

```
对话1（聊天模块）：
  Claude 把角色数据存在 localStorage key "characters"
  消息格式：{ role: 'user' | 'ai', content: string }

对话2（桌宠模块）：
  Claude 把角色数据存在 localStorage key "characterData"  
  消息格式：{ sender: 'user' | 'assistant', body: string }

对话3（设置模块）：
  Claude 把角色数据存在 localStorage key "chars"
  消息格式：{ from: 'user' | 'bot', msg: string }
```

三个模块各自跑都没问题。拼在一起：侧栏读 `characters`，设置写 `chars`，桌宠读 `characterData`——切换角色时名字不更新、头像不更新、聊天记录不更新。用户看到的是"软件有 Bug"，但实际是三个 Claude 各自做了"局部最优"的决定。

**有宪法时的效果**：

`interfaces.md` 定义了唯一的数据类型：
```typescript
interface Character {
  id: string;        // localStorage key: "characters"
  name: string;
  // ...
}

interface Message {
  role: 'user' | 'assistant';  // 不是 'ai' 不是 'bot'
  text: string;                 // 不是 'content' 不是 'body' 不是 'msg'
}
```

`architecture.md` 定义了唯一的数据流：
```
所有数据操作 → core/data-store → localStorage
不允许插件直接操作 localStorage
```

任何 AI 对话，先读 interfaces.md → 知道字段名 → 三个模块天然一致。

### 具体做法

1. **在项目最早期创建这些文件**。不要等"写代码时再补"——那时 AI 已经开始独立决策了。
2. **architecture.md 的"待确定"项要尽早清零**。本文档末尾有 6 个待程序员确认的项，如果一直悬空，AI 遇到这些场景时仍然会自己猜。
3. **interfaces.md 的接口签名变更要走流程**：改本文档 → 搜所有调用方 → 全部更新 → commit 标注 `BREAKING:`。

---

## 三、宪法执行机制：CLAUDE.md

### 是什么

宪法文档写得再好，如果 AI 不读，等于没有。

**CLAUDE.md 是项目根目录下的一个文件，任何 Claude Code 实例在该目录启动时，会自动读取它并注入到系统提示词中。** 这是跨 AI 实例、跨电脑的宪法执行机制。

### 为什么需要它：v0.1.0 的执法漏洞

```
实际情况：
  用户的 Claude    → 知道宪法（用户在对话里反复强调）
  程序员的 Claude  → 不知道宪法（没有 CLAUDE.md，启动时无约束）
  
  程序员对 Claude 说："帮我写个聊天功能"
  程序员的 Claude 视角：
    → 没有 architecture.md → 不知道模块怎么拆
    → 没有 interfaces.md → 不知道 IPC 通道名
    → 没有 design-system.md → 色值硬编码 #FBF9F7
    → 没有 ui-spec.md → 不知道"角色骑在窗口边框上"
  
  结果：代码能跑但全局不兼容 → 假对话 + 设置不存 + 源码脱节
```

有了 CLAUDE.md：

```
  任何人的 Claude 启动 → 自动加载 CLAUDE.md → 被告知"写代码前必须读 docs/"
  → Read docs/architecture.md → 知道模块拆分
  → Read docs/interfaces.md → 知道接口签名
  → Read docs/design-system.md → 知道用 CSS 变量
  → 开始写代码 → 三个模块自然一致
```

### CLAUDE.md 怎么写

**原则：只写"不读这个文件就绝对不知道"的信息。**

✅ 该写的：
- 行为规则（"写代码前必须先读 docs/"）
- 违反过的约束 + 违反它导致的 Bug 编号（"src/ 是唯一真相源。Bug R9 就是这样产生的。"）
- 设计原则（"任何方案必须零用户门槛"）
- 角色分工（"用户=产品/美术，程序员=开发"）

❌ 不该写的：
- 文件清单 — 会过时，让 AI 用 Glob 自己看
- 技术细节 — 在 docs/ 里，去读
- 当前任务 — 会腐烂
- 工作流描述 — 在另一个文件里

**实际案例**：本项目的 CLAUDE.md 从最初 47 行（含 13 个文件路径 + 工作流描述 + 当前任务）精简到 34 行（5 条约束 + 最低必读 3 文件 + 不写什么）。精简原则：每个词都必须是"不读 CLAUDE.md 就不知道"的信息。

### 通用化

这个机制在不同工具中的对应物：

| 工具 | 宪法入口 |
|------|---------|
| Claude Code | `CLAUDE.md`（项目根目录） |
| Cursor | `.cursorrules` |
| GitHub Copilot | `.github/copilot-instructions.md` |

核心原理相同：**宪法不能让用户"记得去读"，而必须让 AI "自动就读"。**

---

## 四、翻译层：让非技术人员和程序员能协作

### 是什么

Claude 充当用户和程序员之间的双向翻译。不是替代任何一方，而是确保信息在传递中不衰减。

### 为什么需要它

**案例：没有翻译层时**

用户说："桌宠要可爱，偶尔眨眼。"

程序员听到后的心理过程：
1. "可爱"是什么意思？圆角？粉色？大眼睛？
2. "偶尔"是多频繁？每分钟一次？每 5 秒一次？
3. "眨眼"是单帧还是动画？几帧？
4. 这个需求到底要写多少代码？

程序员可能会：写一个 CSS animation 让角色图片缩放（他理解的"可爱"= 弹跳效果）——和用户想的完全不一样。做完了用户说"不对"，程序员说"你说清楚啊"，双方都觉得对方不专业。

**有翻译层时**：

| 用户说 | Claude 翻译给程序员 |
|--------|-------------------|
| "桌宠要可爱，偶尔眨眼" | 帧动画组件，2 个动画状态(idle/blink)，blink 叠加在 idle 上，随机定时器触发，间隔 2-5s。blink 用 3-4 帧图层，只画眼睛区域。 |
| "DIY 就是用户能上传角色图调性格" | 上传接口（主进程处理文件系统） + Canvas 裁剪（256×256） + prompt 模板动态注入。性格用模板标签（["温柔","傲娇"...]）+ 自由文本双模式。 |
| "TTS 声音要自然" | TTS 引擎选型——5 方案对比表：音质、成本、延迟、用户门槛。推荐见 TTS 分析文档。 |

反向也翻译：

| 程序员说 | Claude 翻译给用户 |
|---------|----------|
| "用 Electron 打包体积大" | "安装包约 150MB+，加上 TTS 模型后约 5GB，功能不受影响。首次下载可能需要等一会儿。" |
| "IPC 同步有延迟" | "角色跟着窗口移动时可能有一点点拖影，几乎注意不到。" |
| "需要 60fps 渲染管线" | "可以做，加载会多 1 秒左右，能接受吗？" |

### 为什么不能"让程序员直接和用户沟通"

不是沟通能力的问题，是**翻译成本**的问题。程序员需要把自然语言翻译成数据结构的同时还要解释技术约束——每次沟通都是一次小型需求评审。翻译层把这件事系统化：需求来 → 出 spec → 双方对 spec 确认 → 编码。模糊需求在上游就被拦截了。

### 具体做法

1. 用户说需求 → Claude 不直接写代码，先出一段 spec 文字
2. 把 spec 发给程序员审阅（不是发给用户）
3. 程序员提修改 → Claude 更新 spec
4. 双方确认 → 开始编码
5. 用户验收时对着 spec 检查，不看代码

---

## 五、多维决策表：让非技术人员参与技术选型

### 是什么

把每个技术方案的优劣拆成非技术人员也能理解的维度，让产品方能参与决策。

### 案例：TTS 方案选型的曲折过程

**第一轮：Claude 分析 5 个方案**

| 方案 | 难度 | 成本 | 延迟 | 一句话优劣 |
|------|------|------|------|-----------|
| 浏览器 SpeechSynthesis | 1/5 | ¥0 | 即时 | 音质像机器人 |
| Edge-TTS | 2/5 | ¥0 | <1s | 音质好但依赖微软外部接口 |
| VITS 本地模型 | 4/5 | 电费 | <2s | 需 GPU，安装复杂 |
| GPT-SoVITS | 4/5 | 电费 | <3s | 可定制音色但门槛高 |
| 阿里云/讯飞 API | 2/5 | ¥50-200/月 | <0.5s | 稳定音质好但需注册账号Key |

Claude 推荐 Edge-TTS：免费、中文好、20+音色、难度低。

**第二轮：用户试听 → 推翻**

用户试听 Edge-TTS 后："音质不行，像机器人。"

**第三轮：考虑讯飞云 API → 再次推翻**

讯飞音质顶尖，难度低。但需要注册认证获取 Key——违背了项目的核心约束："零用户门槛"（用户不需要注册任何账号）。

**第四轮：最终确定 IndexTTS**

B 站开源，零样本语音克隆——每个角色只需 3-5 秒参考音频即可生成该音色。代价清单：

| 代价 | 应对 |
|------|------|
| 安装包增大 ~3-5GB | 提醒用户；考虑延迟下载模型 |
| 需要 NVIDIA 显卡 | 启动时检测 GPU，无显卡灰掉 TTS + 提示 |
| Python 需打包进 Electron | 子进程 HTTP 服务，localhost 通信 |
| 打包配置复杂 | 主进程管理 Python 生命周期 |

**为什么这个决策过程是"对的"**：

不是因为选了 IndexTTS，而是因为每一步毙掉方案都有明确的理由：
- 毙掉 Edge-TTS → 因为"用户试听音质不达标"（产品决策）
- 毙掉讯飞 → 因为"需要注册 = 违背零门槛原则"（设计原则决策）
- 选择 IndexTTS → 因为"零门槛 + 零成本 + 音质天花板"（原则 + 产品 + 成本三方平衡）

没有多维决策表，这个过程会是：程序员说"用 Edge-TTS"，用户说"不好"，程序员说"那就讯飞"，用户说"要注册太麻烦"——来回拉锯。有了表，每次决策的"为什么"都留痕了。

### 具体格式

每个技术分析文档固定格式：
```
1. 约束条件（列出所有必须满足的条件，如"零用户门槛""中文""免费"）
2. 候选方案表（每个方案一行，维度：难度/成本/延迟/优劣）
3. 推荐路径 + 理由
4. 毙掉的方案 + 毙掉原因（这一条最容易被忽略，但最关键）
5. 决策结果 + 确认日期
```

---

## 六、Spec 作为契约：让非技术人员能验收

### 是什么

每个功能在编码前先写一份 spec（功能规格书）。程序员按 spec 写代码，用户对 spec 验收。Spec 是双方之间唯一的契约。

### 为什么需要它

**案例：没有 spec 时的验收困境**

v0.1.0 开发时，程序员说"桌宠做完了"。用户打开软件——桌宠是一个 CSS 画的简陋圆点脸（两个圆做眼睛 + 一条弧线做嘴），没有角色图、没有动画。程序员认为"功能跑通了 = 完成"，用户认为"能看到角色有动画 = 完成"。

因为没有 spec 定义"桌宠完成"的标准，双方的标准差了十万八千里。

**有 spec 时**：

`development-plan.md` 为 P0 阶段定义了验收标准：
```
验收标准：
- 启动 → 看到主窗口 + 桌宠
- 拖拽窗口边角 → 尺寸变化，布局不崩
- 切换主题 → 色板 + 明暗生效
- 桌宠在桌面显示，可拖拽
```

`ui-spec.md` 定义了更细的交互行为：
```
桌宠生命周期——窗口操作时的行为：
| 用户操作 | 角色行为 | 动画 |
|---------|---------|------|
| 关闭聊天窗口 (✕) | 从边框跳下 → 桌面自由态 | 跳下 250ms ease-out |
| 最小化聊天窗口 | 从边框跳下 → 桌面自由态 | 跳下 250ms ease-out |
```

程序员按表实现，用户按表验收。如果有分歧——"角色关闭窗口后应该回到桌面而不是消失"——回看 spec：spec 写了"关闭窗口→从边框跳下→桌面自由态"。如果 spec 错了就改 spec；如果代码没按 spec 写就改代码。**不存在"我以为你指的是…"这类争议。**

### 具体做法

五步闭环：

```
① 用户用自然语言描述"我想要 XXX"
   ↓
② Claude 翻译成 spec（功能规格书，程序员能直接对着编码）
   ↓
③ 程序员审阅 spec，提出技术约束或修改建议
   ↓
④ 程序员 + Claude 按照 spec 编码（不是按用户原话编码）
   ↓
⑤ 用户对着 spec 验收效果（不看代码）
```

**关键点**：第③步必须程序员确认。如果程序员没确认就开始写，相当于 spec 只是 Claude 的单方面建议，没有契约效力。

---

## 七、素材管线：美术和代码的交接

### 问题

美术素材是跨角色协作中最容易出问题的环节。这个项目中，4 个角色 × 13 个动画 × ~10 帧 = 520 个文件。如果等全部做完再给程序员，开发全部阻塞。如果边做边给，程序员不知道什么时候有什么。

### 解决方案：三层设计

**第一层：规格清单**

每个素材有精确规格，不是"大概做个眨眼动画"，而是：

```
动画：blink（眨眼）
帧数：3-4 帧
时长：~0.3s 单次
触发：叠加在 idle 上，随机触发（间隔 2-5s）
格式：PNG 序列帧，透明背景
尺寸：256×256 画布，角色居中偏下
命名：{角色名}_blink_{帧号}.png
```

程序员拿着这个清单，**即使素材还没做出来，也可以先把代码框架写好**（用占位图），等素材到位只需要替换文件路径。

**第二层：捷径方案**

承认"全部做完"不现实，提供降级路径：

| 简化项 | 做法 |
|--------|------|
| walk 不画 | 角色原地 idle，拖拽后瞬移到新位置 |
| 左右翻转 | 只画向右，向左用 CSS `scaleX(-1)` |
| 错误表情 | sad/silent/think 简化成 1-2 帧静态 |
| react 合并 | 只做一种 react（开心），随机换台词 |

捷径后：520 文件 → 164 文件 → 先做一个角色只需 ~38 帧。

**第三层：分层画法**

```
表情层（独立绘制）
  ├── blink → 只用眼睛区域 4 帧
  └── talk  → 只用嘴巴区域 4 帧
─────────────────────────
身体层（循环播放）
  └── idle → 独立 8-12 帧循环
```

好处：blink 和 talk 不用重画整个身体，只画表情部位。省大量帧数。

### 为什么这样设计

素材管线的核心不是"告诉美术做什么"，而是**让程序员在素材到位前能工作，素材到位后零摩擦接入**。三个原则：

1. **规格先于素材**：清单比素材先出，代码框架比素材先搭
2. **捷径保证流程跑通**：不全量不阻塞，先跑通再补全
3. **一个角色跑通再复制**：不在 4 个角色上同时踩坑

---

## 八、风险分级与阻塞关系图：知道先修什么

### 问题

Bug 审查发现了 15 项问题，严重程度不同，互相还有依赖关系。如果按"发现的顺序"修，可能修了 10 个低优先级 Bug 才发现致命 Bug 还没修——白费功夫。

### 解决方案

**第一层：四级严重度**

```
🔴 致命（3 项）：假对话、设置不存、代码连贯性崩溃
🟠 高（4 项）：素材进度阻塞、无首次启动引导、无主题系统、桌宠挡住鼠标
🟡 中（3 项）：退出不保存、源码与构建脱节、打包体积
🟢 低（2 项）：集成显卡/TTS、上传图片无处理
```

**第二层：阻塞关系图**

这是整个风险评估的核心创新——不是列清单，而是画依赖拓扑：

```
素材制作 (R4) ──阻塞──▶ P0 桌宠框架
                    ──阻塞──▶ P1 动画交互
                    ──阻塞──▶ P2 完整动画状态机

源码脱节 (R9) ──阻塞──▶ 所有 Bug 修复
假对话 (R2)  ──阻塞──▶ P1 聊天核心
设置不存 (R3) ──阻塞──▶ P1 API 接入 / P3 设置+DIY
```

### 为什么阻塞关系图比 Bug 清单更有用

**没有阻塞关系图时的典型错误**：

看见 15 个 Bug → 从简单的开始修 → 修了"退出不保存"(R8) → 修了"图片无裁切"(R12) → 修了"模型仅 3 个"(R14) → 3 天过去了 → 发现 R2（假对话）还没修 → 而这个 Bug 导致聊天功能完全不可用 → 之前修的 Bug 是基于"假对话环境"修的 → 修完 R2 后前面修的 R8/R12/R14 可能需要返工。

**有阻塞关系图时**：

一看就知道 R2 + R3 + R9 阻塞了所有下游工作 → 先把这三个修了 → 其他 Bug 的修复才有意义。

### 具体做法

1. 每次 Bug 审查后不只是列清单，而是画阻塞关系
2. 格式：`{阻塞项} ──阻塞──▶ {被阻塞项}`
3. 提问"这个 Bug 不修的话，哪些工作做了也是白做？"
4. 答案就是阻塞关系的箭头指向

---

## 九、活文档与 Git 自动同步

### 问题

传统文档的问题是"写完就死"——写的时候认真，两周后过时，没人维护。因为维护文档是额外动作。

### 解决方案

**用 Git 钩子让文档自动活在版本管理中**：

| 时机 | 自动动作 |
|------|---------|
| Claude 编辑/写入文件后 | `git add → commit → push` |
| Claude 对话开始前 | `git pull`（拉取对方最新改动） |

**效果**：
- 这个项目已有 30+ 次提交，覆盖从规划到 Bug 审查的完整过程
- `git log` 是可追溯的决策时间线：TTS 从 Edge 到 IndexTTS 的决策链路完整可查
- 改错了可以 `git revert`
- 两台电脑之间实时同步

**Why Git 而不是共享文件夹/云盘**：

共享文件夹没有历史版本。云盘有冲突。Git 天然解决了两个问题：
1. 多台电脑修改同一个文件时自动合并（文本文件）
2. 每次修改都有作者和时间戳

### 具体配置

在项目根目录 `.claude/settings.json` 中：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{
          "type": "command",
          "command": "git add -A; git commit -m \"auto: Claude 更新文档\"; git push"
        }]
      }
    ],
    "SessionStart": [
      {
        "hooks": [{
          "type": "command",
          "command": "git pull"
        }]
      }
    ]
  }
}
```

### 通用化到其他工具

在 Claude Code 之外的场景，可以用任何自动化实现同样效果：
- 文件保存 → 自动 git commit
- 定时任务 → `git pull`
- 不限工具，原理一致

---

## 十、流程自更新

### 问题

传统工作流的问题：定义流程的人（项目经理）不是执行流程的人（一线），修改流程要开会。流程过时不适用了，但没人去改。

### 解决方案

工作流规则本身也放在 Git 仓库里（`协作工作流框架.md` 或 `workflow.md`）。任何一方觉得流程不对 → 告诉 Claude → Claude 更新文件 → 自动 commit+push → 下周按新规则执行。

**不需要开会。不需要审批。流程和代码一样可迭代。**

### 为什么这很重要

如果改流程的成本是"开一次会 + 写邮件 + 同步全员"，那流程就会腐烂——每个人都在默默忍受不合适的流程。如果改流程的成本是"告诉 Claude 一句话"，流程就会越来越贴合实际工作。

### 实际案例

本项目的协作工作流框架从最初版本到 v2，经历了至少 3 次自更新：
- v1：只有翻译层 + 周报
- v2：加入五步开发法、技术方案分析表、项目节奏、Git 多机互通、CLAUDE.md 执法机制

每次更新都是因为前面的版本在实际使用中暴露了问题。没有一次需要"开会讨论流程"。

---

## 十一、完整落地清单：如何在新项目中搭建这套体系

### 第一步：创建宪法入口

在项目根目录创建 `CLAUDE.md`（或对应工具的等效文件）。

**写到这个文件里的内容判断标准**：这段信息是否无法从代码或目录结构中自动获取？

**示例内容**（最少版本）：
```markdown
# CLAUDE.md

本项目是 {产品一句话描述}。{角色A}负责 {职责}，{角色B}负责 {职责}。
Claude 的角色是 {具体定义}。

## 强制规则

写代码前必须先读取 docs/ 目录下的相关文件。
最低每次必读：docs/architecture.md、docs/interfaces.md、docs/design-system.md。

## 不可违反的约束

1. {约束1} — Bug R{X} 就是这样产生的
2. {约束2} — ...
```

### 第二步：创建宪法文档

按优先级：

1. **vision.md** — 让 AI 知道"这个产品是什么"。一句话 + 目标用户 + 竞品差异。不需要长。
2. **architecture.md** — 技术栈、模块树、数据流。标注所有"待确定"项并设 deadline。
3. **interfaces.md** — 所有跨模块通信的接口签名。这是宪法中最重要的文件——不一致的根因几乎都在这。
4. **design-system.md** — 所有视觉 token。色值、间距、动效精确到像素。**不允许 AI 自己选值。**
5. **features.md** — 功能清单 + 优先级。让 AI 知道什么该做什么不该做。

### 第三步：建立 spec 流程

每个新功能：
1. 创建 `specs/{功能名}-analysis.md`
2. 写多维对比表（如果涉及技术选型）
3. 写验收标准（具体可检查的项）
4. 程序员审阅确认
5. 开始编码

### 第四步：配置自动同步

在 `.claude/settings.json` 中配置 Git 钩子（或在其他工具中找等效机制）。

### 第五步：每次 Bug 审查后更新风险登记表

不仅是列 Bug，而是：
- 四级严重度
- 阻塞关系图
- 每个风险的应对方案

### 各步骤的优先级

```
必须做（否则必崩）：
  ├── CLAUDE.md（宪法入口）
  ├── architecture.md + interfaces.md（宪法核心）
  └── design-system.md（宪法核心）

应该做（大幅降低返工）：
  ├── 翻译层（spec 流程）
  ├── 多维决策表
  └── Git 自动同步

做了更好（长期收益）：
  ├── 风险分级 + 阻塞图
  ├── 流程自更新
  └── 自动化周报
```

---

> **最后更新**：2026-06-06
> **版本**：1.0
> 
> 本文档本身也是这套体系的一部分——如果某个部分在实际使用中发现不对，直接改。
