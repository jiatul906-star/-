# AI 伴侣 开发速查手册

> 生成时间：2026-07-08 | 供下次会话快速恢复上下文

## 一句启动

```
cd C:/Users/35139/jiatul906-star-repo && git config http.proxy http://127.0.0.1:33210 && 读取 bug/开发速查手册.md 恢复上下文
```

> **Git 代理 + push 踩坑**：代理 `http://127.0.0.1:33210` 会导致 GitHub push SSL 失败，push 时必须绕过代理：
> ```bash
> git -c http.proxy= push
> ```
> 
> **初始数据恢复机制**：`ensureDefaultCharacters()` 检测 `character/` 为空时，从 `magins/default-characters/` 复制初始数据到 `%APPDATA%/with-u/character/`。开发环境读项目目录，生产环境读 `process.resourcesPath`。

---

## 当前状态快照

- **版本**：0.1.0，已提交 `11cf74c`，已推送 origin/main
- **初始数据已打包**：`magins/default-characters/` 含 4 个角色（Alice/Amy/Mary/Noma）+ 立绘/头像 + 视频，首次启动或删除数据后自动恢复
- **应用已启动**：3 窗口（聊天 960×680 / 桌宠 160×270 / 设置 780×560）
- **角色文件夹化已完成**：`%APPDATA%/with-u/character/{角色名}/` 下各自存放 config.json / portrait.png / avatar.png / chat-history.json / agent-memory.json / video/
- **空闲视频播放已实现**：3s 首播 / 5-10s 循环间隔，随机播 video/ 中的 mp4
- **小桃 10 个 mp4**，**小蓝 8 个 mp4**

## 构建命令

```bash
cd C:/Users/35139/jiatul906-star-repo
# 构建 + 启动（必须用完整路径，git bash PATH 不含 node）
PATH="/c/Program Files/nodejs:$PATH" "/c/Program Files/nodejs/npx.cmd" electron-vite build
PATH="/c/Program Files/nodejs:$PATH" "/c/Program Files/nodejs/npx.cmd" electron .
# 杀进程（注意 git bash 中 // 会被转义，用 -F 代替）
/c/Windows/System32/taskkill.exe -F -IM electron.exe
```

## Git 命令（需要完整路径 + 绕过代理 push）

```bash
git_exe="/c/Program Files/Git/cmd/git.exe"
$git_exe -C "C:/Users/35139/jiatul906-star-repo" add -A
$git_exe -C "C:/Users/35139/jiatul906-star-repo" commit -m "..."
$git_exe -C "C:/Users/35139/jiatul906-star-repo" -c http.proxy= push  # push 必须绕过代理
```

---

## 关键架构变化（2026-07-08）

### 数据存储：从平铺 JSON → 按角色文件夹

```
%APPDATA%/with-u/character/
├── _index.json              ← {activeId, entries: {[id]: {id, folderName}}}
├── 小桃/
│   ├── config.json          ← 单角色配置（无 base64 图片）
│   ├── portrait.png         ← 立绘
│   ├── avatar.png           ← 头像
│   ├── chat-history.json    ← 聊天记录
│   ├── agent-memory.json    ← 人设/记忆
│   └── video/               ← 动作视频 + 空闲播放视频
└── 小蓝/（同上）
```

### 类型变化

- `CharacterConfig` **已移除** `imageDataUrl` / `avatarDataUrl`
- 运行时图片：`characterPortraits[charId]` / `characterAvatars[charId]`（Zustand 缓存）
- `PetAction.videoPath` 现在是纯文件名，不是绝对路径

### 新增 IPC

| 通道 | 入参 | 返回 |
|------|------|------|
| `dialog:openImage` | `charName, imageType` | dataUrl \| null |
| `pet-image:getCurrent` | `charName, imageType` | dataUrl \| null |
| `dialog:openVideo` | `charName` | 文件名 \| null |
| `character:getVideoPath` | `charName, fileName` | 完整路径 \| null |
| `character:listVideos` | `charName` | `string[]`（文件名） |
| `dialog:openCharacterVideoFolder` | `charName` | void |

### 空闲视频播放

- PetWindow 空闲时自动随机播放 `character/{name}/video/` 中的 mp4
- 间隔：首次 3s，循环 5-10s 随机
- 防护：菜单打开/拖拽/动作视频播放/聊天中 → 不触发

---

## 一、项目骨架

```
C:/Users/35139/jiatul906-star-repo/
├── src/
│   ├── main/                    # Electron 主进程
│   │   ├── index.ts             # 入口：创建窗口、注册 IPC
│   │   ├── ipc/index.ts         # ⭐ IPC 通道注册 + 持久化 + 迁移
│   │   └── windows/             # chat.ts / settings.ts / pet.ts
│   ├── preload/                 # contextBridge → window.electronAPI
│   │   ├── index.ts             # ⭐ 所有 API 桥接
│   │   └── index.d.ts           # TypeScript 类型声明
│   ├── renderer/
│   │   ├── main.tsx             # React 入口
│   │   ├── App.tsx              # 根组件（多窗口路由）
│   │   ├── plugins/chat/api.ts  # ⭐ AI API（buildSystemPrompt + streamChat）
│   │   ├── stores/pet-store.ts  # ⭐ Zustand 全局状态
│   │   └── components/
│   │       ├── ChatWindow.tsx   # ⭐ 大聊天窗口
│   │       ├── SettingsWindow.tsx # ⭐ 设置窗口
│   │       ├── PetWindow.tsx    # ⭐ 桌宠窗口（空闲视频 + 右键聊天）
│   │       ├── ContextMenu.tsx  # ⭐ 右键环形菜单
│   │       ├── ActionEditor.tsx # 动作编辑器
│   │       ├── ChromaKeyVideo.tsx
│   │       └── *.css
│   └── common/types.ts          # ⭐ 所有公共类型
├── magins/video/                # 视频素材
├── out/                         # 构建产物
└── bug/                         # Bug 记录 + 本手册
```

---

## 二、构建 & 启动命令

```bash
cd C:/Users/35139/jiatul906-star-repo

# 构建（必须用完整路径，git bash 中 PATH 不包含 node）
PATH="/c/Program Files/nodejs:$PATH" "/c/Program Files/nodejs/npx.cmd" electron-vite build

# 启动
PATH="/c/Program Files/nodejs:$PATH" "/c/Program Files/nodejs/npx.cmd" electron .

# 杀进程
"/c/Windows/System32/taskkill.exe" //F //IM electron.exe
```

---

## 三、数据持久化（IPC 通道）

```typescript
// ===== 角色 =====
window.electronAPI.getCharacters()                    // → CharactersData
window.electronAPI.saveCharacters({ characters, activeId })
window.electronAPI.onCharactersUpdated(callback)

// ===== 图片 =====
window.electronAPI.openImageDialog(charName, 'portrait' | 'avatar')  // → dataUrl
window.electronAPI.getPetImage(charName, 'portrait' | 'avatar')       // → dataUrl
window.electronAPI.onPetImageUpdated(callback)  // payload: {charId, charName, imageType, dataUrl}

// ===== 视频 =====
window.electronAPI.openVideoDialog(charName)           // → 文件名
window.electronAPI.getVideoPath(charName, fileName)    // → 完整路径
window.electronAPI.listIdleVideos(charName)            // → string[]
window.electronAPI.openCharacterVideoFolder(charName)

// ===== API Profiles =====
window.electronAPI.getApiProfiles() / saveApiProfiles() / testApiConnection()

// ===== 聊天历史 =====
window.electronAPI.getChatHistory(characterId) / addChatMessage() / clearChatHistory()

// ===== 智能体记忆 =====
window.electronAPI.getAgentMemory(characterId) / addAgentMemory() / deleteAgentMemory() / updateAgentMemory()
window.electronAPI.exportAgentMemory(characterId, charName) / importAgentMemory(characterId)

// ===== 窗口控制 =====
window.electronAPI.close() / minimize() / maximize() / openChat() / openSettings()
window.electronAPI.resizePet(expand, scrCX, scrCY) / movePet(dx, dy)
```

---

## 四、常见 Bug & 避坑

| # | 问题 | 原因 | 修复 |
|---|------|------|------|
| 1 | 修改源码不生效 | 未 build | 重新 `electron-vite build` |
| 2 | 设置页输入无效 | 未绑定 value/onChange | 状态绑定 → `updateCharacter` → persist 三步 |
| 3 | JSX 中文引号报错 | 被当成属性分隔符 | `{'...'} ` 包裹 |
| 4 | framer-motion exit 慢 | 共享 transition | variants 给 exit 设 `{duration:0.1}` |
| 5 | 关闭菜单后消息残留 | `setChatMessages` 未清空 | `setChatMessages([])` |
| 6 | "你是谁"被当成设定 | `/^你是(.+)$/` 太宽泛 | 疑问词黑名单过滤 |
| 7 | 图片/视频不显示 | charName 给错了 | openImageDialog 现在要传 `charName` 不是 `charId` |
| 8 | 视频去底/裁切无法加载 | videoPath 是文件名不是完整路径 | `handleOpenCropper`/`handleOpenChromaPicker` 先调 `getVideoPath` 解析 |
| 9 | `taskkill //F` 报错"无效参数" | git bash 中 `//` 被当路径 | 改用 `-F -IM`（减号格式） |
| 10 | git push SSL 失败 | 代理 `http://127.0.0.1:33210` 干扰 | `git -c http.proxy= push` 绕过代理 |

---

## 五、已修复 Bug 记录（2026-07-08）

| 问题 | 根因 | 修复文件 |
|------|------|---------|
| 视频去底弹窗无法加载视频 | `VideoChromaPicker` 接收纯文件名，无法构建 file:// URL | ActionEditor.tsx：打开弹窗前先 `resolveVideoPath` 解析完整路径 |
| 裁切弹窗无法加载视频 | `VideoCropper` 同样问题，且新增模式下仍用旧代码 | ActionEditor.tsx：新增模式也改用 `handleOpenCropper` |
| 视频播放区太小 | 裁切 `maxHeight:300`，去底 `maxHeight:180` | VideoCropper.tsx→520，VideoChromaPicker.tsx→360 |
| 视频去底"裁切"标签消失 | — | 确认功能正常，裁切/去底可独立使用或组合使用 |
