# 风险登记表

> 最后更新：2026-06-05 | 来源：bug 审查 + 开发计划

---

## 🔴 致命风险

| # | 风险 | 影响 | 当前状态 | 应对 |
|---|------|------|----------|------|
| R1 | **代码连贯性崩溃** | 上次项目因此崩盘——AI 辅助代码缺乏连贯性，src/ 与构建产物脱节，无法迭代维护 | ⚠️ 已发生 | Claude 每次写代码前强制读 docs/；src/ 必须与构建产物同步 |
| R2 | **AI 对话是假的** | 核心功能不存在，产品无价值。所有回复从 6 条固定文案随机选，API 完全未接入 | 🔴 致命 Bug | 重写 sendMessage，对接 API 地址/Key/模型 |
| R3 | **设置完全不持久化** | 用户所有配置（API Key、性格、模型选择等）输入即丢弃，无法正常使用 | 🔴 致命 Bug | 绑定 value/onChange，实现本地持久化 |

---

## 🟠 高风险

| # | 风险 | 影响 | 当前状态 | 应对 |
|---|------|------|----------|------|
| R4 | **桌宠素材进度慢** | P0-P2 全部阻塞，无素材则桌宠只显示 CSS 简陋圆点脸 | ⚠️ 13个动画×4角色=0%完成 | 捷径方案：静态帧 + CSS flip；先做一个角色跑通流程 |
| R5 | **无首次启动引导** | 用户打开后一脸茫然，不知道能做什么、怎么开始 | ❌ 未实现 | P3 实现 4 角色横排卡片 + 确认流程 |
| R6 | **无主题系统** | 设计稿 5主题×2明暗=10套配套色，代码全硬编码浅色值 | ❌ 未实现 | P0 用 CSS 变量搭建主题系统 |
| R7 | **桌宠挡住桌面鼠标** | setIgnoreMouseEvents(false) + 缺少 pointer-events:auto，桌宠窗口区域桌面不可点击 | ⚠️ Bug | 修正穿透逻辑 |

---

## 🟡 中风险

| # | 风险 | 影响 | 当前状态 | 应对 |
|---|------|------|----------|------|
| R8 | **退出不保存** | Ctrl+Shift+Q 直接 app.exit(0)，无确认无清理无持久化，数据丢失 | ⚠️ Bug | before-quit 实现保存逻辑 + 退出确认对话框 |
| R9 | **源码与构建脱节** | src/ 仅 React.StrictMode 空壳，实际逻辑全在 out/ ASAR 产物中，无法基于 src 修 bug | ⚠️ Bug | 代码回迁到 src/，以 src 为唯一真相源 |
| R10 | **Electron + IndexTTS 打包体积** | IndexTTS 模型 ~2.3G，首次安装包过大影响用户下载意愿 | 待验证 | 提示用户；考虑模型延迟下载（首次启用 TTS 时拉取） |

---

## 🟢 低风险

| # | 风险 | 影响 | 当前状态 | 应对 |
|---|------|------|----------|------|
| R11 | **集成显卡不支持 TTS** | 无 NVIDIA GPU 用户无法使用 TTS 功能 | 设计已覆盖 | GPU 检测 → 无显卡时灰掉 TTS + 提示 |
| R12 | **上传图片无处理** | DIY 角色图直接原图使用，无裁剪缩放，可能出现变形或过大 | ❌ 未实现 | P3 加入裁剪/缩放/预览 |

---

## 阻塞关系

```
素材制作 (R4) ──阻塞──▶ P0 桌宠框架
                    ──阻塞──▶ P1 动画交互
                    ──阻塞──▶ P2 完整动画状态机

源码脱节 (R9) ──阻塞──▶ 所有 Bug 修复
假对话 (R2)  ──阻塞──▶ P1 聊天核心
设置不存 (R3) ──阻塞──▶ P1 API 接入 / P3 设置+DIY
```

---

## 总结

- **致命**：3 项（假对话、设置不存、代码连贯性崩溃）
- **高**：4 项（素材进度、引导缺失、主题缺失、鼠标穿透）
- **中**：3 项（退出不保存、源码脱节、打包体积）
- **低**：2 项（集显/TTS、图片无裁切）
- **首要解决**：R2 + R3 + R9（不解决则后续开发无意义）
