# 协作工作流框架 v2

> **状态：起草中** | 最后更新：2026-05-13

## 核心理念

Claude 是用户和程序员之间的"翻译层"和"连贯性保障"——不是替代任何一方，而是确保信息不丢失、设计不偏离。

---

## 一、项目知识库 (docs/)

在代码仓库中，**先建文档再写代码**。每次 AI 写代码前必须读取这些文件：

```
docs/
├── vision.md          # 产品愿景：这个工具长什么样、给谁用
├── features.md        # 功能清单 + 优先级排序
├── architecture.md    # 技术架构：技术栈、模块拆分、数据流
├── interfaces.md      # 模块间接口约定：每个模块的输入/输出
├── ui-spec.md         # UI 规格：界面布局、组件、交互定义
├── design-system.md   # 设计系统：色彩、字体、间距、组件规范
└── workflow.md        # 本工作流规则（可随时修改）
```

---

## 二、五步开发法

每个功能严格按以下流程执行，不跳步：

```
Step 1: 用户用自然语言描述"我想要 XXX"
        ↓
Step 2: Claude 将需求翻译成 spec（功能规格书），程序员可直接对着写代码
        ↓
Step 3: 程序员审阅 spec，提出技术约束或修改建议
        ↓
Step 4: 程序员 + Claude 协作写代码，Claude 确保代码符合 architecture.md
        ↓
Step 5: 用户对着 spec 验收效果，而不是看代码
```

**为什么能防崩**：之前 AI 每次写的代码"局部最优但全局不兼容"，因为没有 architecture.md 做约束。现在每次写代码前先读宪法。

---

## 三、翻译层

Claude 负责将双方的语言互相翻译：

| 用户说 | Claude 翻译给程序员 |
|--------|-------------------|
| "桌宠要可爱，偶尔眨眼" | 帧动画组件，2个动画状态(idle/blink)，触发用随机定时器 |
| "DIY 就是用户能上传角色图调性格" | 上传接口 + 图片裁剪 + prompt 模板动态注入 |
| "TTS 声音要自然" | TTS 引擎选型建议 + 流式播放接口要求 |

| 程序员说 | Claude 翻译给用户 |
|---------|-----------------|
| "用 Electron 打包体积大" | "安装包约 150MB+，功能不受影响" |
| "这个需要 60fps 渲染管线" | "可以做，加载多 1 秒，能接受吗？" |

---

## 四、技术方案分析 (specs/)

每个功能在做之前，Claude 出具技术分析表：

```
specs/
├── tts-analysis.md     # TTS 方案对比
├── diy-analysis.md     # DIY 功能方案对比
└── mascot-analysis.md  # 桌宠方案对比
```

### 分析表格式

以 TTS 为例：

| 方案 | 描述 | 难度(1-5) | 月成本 | 延迟 | 优劣 |
|------|------|-----------|--------|------|------|
| 浏览器 SpeechSynthesis | 免费零依赖 | 1 | ¥0 | 即时 | 音质差，不可控 |
| Edge-TTS | 微软免费接口 | 2 | ¥0 | <1s | 音质好，依赖外部 |
| VITS 本地模型 | 开源TTS | 4 | 电费 | <2s | 需GPU，安装复杂 |
| GPT-SoVITS | 少样本克隆 | 4 | 电费 | <3s | 可定制角色音色 |
| 阿里云/讯飞 API | 商用云服务 | 2 | ¥50-200/月 | <0.5s | 稳定音质好 |

每个方案附推荐路径和理由。

---

## 五、每周工作报告 (reports/)

每周日 Claude 自动生成，固定格式：

```
reports/
├── 2026-W20.md
├── 2026-W21.md
└── ...
```

### 周报格式

```
## 工作周报 — 2026年第X周 (日期范围)

### 本周完成
- [功能A] 已完成，验收通过

### 本周未完成 / 阻塞
- [功能C] 原因

### 下周计划
- 程序员：XXX
- 用户：XXX
- Claude：XXX

### 风险提示
- 潜在延期或技术风险

### 决策记录
- 日期: 决策内容
```

周报内容来源于 docs/ 中各 spec 的状态，不是凭空编写。

---

## 六、项目节奏

| 节奏 | 做什么 |
|------|--------|
| **启动会** | 三人讨论 vision.md 和 features.md，定 MVP 范围 |
| **周例会** | 回顾上周 → 定本周任务 → 用户验收 |
| **功能闭环** | spec → 代码 → 验收 → 标记完成 |
| **代码评审** | 程序员每完成模块，Claude 自查是否符合 architecture.md |

---

## 七、工作流自更新

- `docs/workflow.md` 是本工作流规则文件
- 任何一方觉得流程不适用，直接修改该文件
- 修改方式：用户/程序员告诉 Claude → Claude 更新 workflow.md
- 下一周按新规则执行

---

## 八、完整目录结构

```
project/
├── .claude/
│   └── settings.json     # Claude Code 钩子配置
├── docs/
│   ├── workflow.md
│   ├── vision.md
│   ├── features.md
│   ├── architecture.md
│   ├── interfaces.md
│   ├── ui-spec.md
│   └── design-system.md
├── specs/
│   ├── tts-analysis.md
│   ├── diy-analysis.md
│   └── mascot-analysis.md
├── reports/
│   ├── 2026-W20.md
│   └── ...
└── assets/
    └── ...
```

---

## 九、多机互通：Git 仓库配置

两台电脑之间通过 Git 仓库实现 Claude Code 产出文件的实时互通和历史记录。

### 9.1 创建仓库

在 GitHub 或 Gitee 上创建私有仓库（推荐），或在局域网内搭建裸仓库。

#### 方案 A：GitHub / Gitee 托管（推荐）

```
# 电脑A上，在项目文件夹初始化
cd E:\claude code\AI聊天工具工作流
git init
git add -A
git commit -m "初始化项目工作流"
git remote add origin https://github.com/你的用户名/ai-chat-workflow.git
git push -u origin main
```

```
# 电脑B上，克隆仓库
cd D:\你的工作目录
git clone https://github.com/你的用户名/ai-chat-workflow.git
cd ai-chat-workflow
```

#### 方案 B：局域网裸仓库（无需云端，同一网络可用）

```
# 电脑A（创建裸仓库作为服务器）
mkdir E:\shared\ai-chat.git
cd E:\shared\ai-chat.git
git init --bare

# 电脑A 关联远程
cd E:\claude code\AI聊天工具工作流
git remote add origin E:\shared\ai-chat.git
git push -u origin main

# 电脑B 克隆
git clone file:////电脑A的IP或主机名/shared/ai-chat.git
```

> 局域网方案需要共享文件夹权限，且两台电脑需在同一网络。

### 9.2 配置 Git 用户信息

两台电脑分别执行：

```
git config user.name "你的名字"
git config user.email "你的邮箱"
```

### 9.3 配置免密推送

```
git config credential.helper store
```

首次 `git push` 时输入密码后，后续无需重复输入（GitHub token 方式同理）。

### 9.4 配置 Claude Code 钩子

在项目根目录创建 `.claude/settings.json`：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{
          "type": "command",
          "command": "git add -A; git commit -m \"auto: Claude 更新文档 $(Get-Date -Format 'yyyy-MM-dd HH:mm')\"; git push"
        }]
      }
    ],
    "SessionStart": [
      {
        "hooks": [{
          "type": "command",
          "command": "git pull"
        }]
      }
    ]
  }
}
```

> 注意：Windows 上钩子命令使用 PowerShell 语法，macOS/Linux 需将 `Get-Date` 替换为 `date`。

### 9.5 效果

| 时机 | 自动动作 |
|------|---------|
| Claude 编辑/写入文件后 | `git add → commit → push` |
| Claude 对话开始前 | `git pull`（拉取对方最新改动） |
| 需要查看历史 | `git log` 查看所有变更记录 |
| 改错需要回退 | `git revert` 或 `git reset` 回退到任意版本 |

### 9.6 日常使用

- 两台电脑都启动 Claude Code，正常对话
- 每次写文件自动推送，对话开始自动拉取
- `git log --oneline` 查看变更历史
- 一方先改完推送，另一方自动拉到，不存在冲突

---

## 十、宪法执行机制：CLAUDE.md

### 10.1 问题

宪法文档（`docs/architecture.md` 等）存在 Git 仓库中，但**对方的 Claude 启动时不会自动读取它们**。Claude Code 只会自动读取项目根目录的 `CLAUDE.md`。

如果缺少 `CLAUDE.md`，就会发生：
- 用户的 Claude 遵守宪法（因为用户在对话里要求）
- 程序员的 Claude 不知道宪法的存在
- 程序员说"帮我写个聊天功能"，Claude 直接写——不读 architecture.md、不读 interfaces.md
- 结果：代码"能跑但全局不兼容"→ 和上次崩盘一模一样的模式

### 10.2 解决方案

在项目根目录创建 `CLAUDE.md`。**任何** Claude Code 实例在此目录启动时，会自动读取它并注入到系统提示词中。

```markdown
# CLAUDE.md
强制规则：每次写代码前，必须先读取以下文件。不读不写。

1. docs/architecture.md
2. docs/interfaces.md  
3. docs/design-system.md
...
```

### 10.3 原理

```
没有 CLAUDE.md 时：
  用户的 Claude    ──✅ 知道宪法（手动告知）
  程序员的 Claude  ──❌ 不知道宪法 → 自由发挥 → 崩盘

有了 CLAUDE.md 时：
  用户的 Claude    ──✅ 自动加载 CLAUDE.md → 强制执行宪法
  程序员的 Claude  ──✅ 自动加载 CLAUDE.md → 强制执行宪法
```

### 10.4 通用化

这是整个治理框架最关键的一环。多人 + 多 AI 协作时，必须有一个**自动加载、无需手动操作**的宪法入口：

| 工具 | 宪法入口 |
|------|---------|
| Claude Code | `CLAUDE.md`（项目根目录） |
| Cursor | `.cursorrules` |
| GitHub Copilot | `.github/copilot-instructions.md` |

选择哪个取决于工具，但原理相同：**宪法不能让用户"记得去读"，而必须让 AI "自动就读"。**
