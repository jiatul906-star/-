# 工作流方法论总结：从 AI 聊天伴侣开发中提炼的可迁移框架

> 最后更新：2026-06-06

---

## 目录

1. [项目背景](#一项目背景)
2. [完整开发历程](#二完整开发历程)
3. [遇到的每个问题及其解决方案](#三遇到的每个问题及其解决方案)
4. [治理框架：可迁移到任何项目的结构](#四治理框架可迁移到任何项目的结构)
5. [当前状态与下一步](#五当前状态与下一步)

---

## 一、项目背景

### 产品

面向**零技术基础用户**的桌面 AI 聊天伴侣。核心功能：AI 拟人对话、DIY 角色创建、TTS 语音（零样本克隆）、桌宠（桌面悬浮动画角色）、多角色管理。

技术栈：Electron + React + IndexTTS（本地 AI 模型 ~2.3GB）。

### 团队

| 角色 | 职责 |
|------|------|
| 用户（产品/美术） | 素材制作、UI 设计验收、需求决策、配音 |
| 程序员 | 代码编写、技术决策、架构实现 |
| Claude | 翻译需求、保障连贯性、编写 spec、辅助编程、自动生成文档/周报 |

### 起点：一个已经崩溃的 v0.1.0

在开始这套工作流之前，程序员已经用 AI 辅助开发过一版。它能跑，但加入 DIY 功能时代码彻底崩溃。根本原因：**AI 每次写的代码"局部最优但全局不兼容"**——模块间接口不一致、状态管理混乱、架构缺乏约束。

决定：**不修旧代码，重新开始，以 Claude 作为协作核心。**

---

### ⚠️ 宪法的执法漏洞（v0.1.0 崩溃的根因补充分析）

**问题：对方的 Claude 不会自动遵守你的宪法。**

宪法文档（`docs/architecture.md` 等）存在 Git 仓库中，但程序员的 Claude Code 启动时**不会自动读取它们**。Claude Code 只会自动读取项目根目录的 `CLAUDE.md`。

v0.1.0 的致命 Bug（假对话、设置不存、源码脱节）之所以发生，不是因为宪法写得不好，而是因为**宪法没有被对方的 Claude 执行**：

```
实际情况：
  用户的 Claude    → 知道宪法（用户在对话里反复强调）
  程序员的 Claude  → 不知道宪法（没有 CLAUDE.md，启动时无约束）
  
  程序员对 Claude 说："帮我写个聊天功能"
  程序员的 Claude → 直接写（没有读 architecture.md，没有读 interfaces.md）
                  → 用固定文案替代 API 调用（不知道"零门槛"不等于"零功能"）
                  → 表单不绑定 value（不知道设计系统要求持久化）
                  → 代码堆在 ASAR 里（不知道 src/ 是唯一真相源）
```

**解决方案：`CLAUDE.md` — 跨 Claude 实例的宪法执行机制**

在项目根目录创建 `CLAUDE.md`。任何 Claude Code 实例在此目录下启动时，都会自动读取它并注入到系统提示词中。

```markdown
# CLAUDE.md
强制规则：每次写代码前，必须先读取以下文件。不读不写。

1. docs/architecture.md
2. docs/interfaces.md
3. docs/design-system.md
...
```

**这是整个治理框架最关键的一环**。没有它，宪法只是一堆好看的 markdown 文件——你自己的 Claude 遵守，但对方的不遵守。有了它，任何人的 Claude 在进入这个项目时都带着同样的约束。

**通用化**：多人 + 多 AI 协作时，必须有一个**自动加载、无需手动操作**的宪法入口。Claude Code 用 `CLAUDE.md`，Cursor 用 `.cursorrules`，GitHub Copilot 用 `.github/copilot-instructions.md`。选择哪个取决于工具，但原理相同——它不能让用户"记得去读"，而必须让 AI "自动就读"。

---

## 二、完整开发历程

### 阶段 1：规划（5 月 12-14 日）—— 先定义"做什么"和"用什么做"

**做的事**：
- 创建 `docs/vision.md`：一句话产品描述、目标用户画像、与竞品（Character.AI / ChatGPT）的差异
- 创建 `docs/features.md`：5 大功能模块 + 子功能拆解 + 原则声明（"不做 MVP 删减""UI 可迭代""DIY 高自由度"）
- 创建 `specs/architecture-analysis.md`：三种架构方案对比

**关键决策过程——架构选型**：

三种方案摆在桌上：

```
方案A（独立模块）：聊天、桌宠、DIY 各自独立小程序，共享数据层
  优点：一个崩了不影响其他
  缺点：体验割裂，用户感觉是"几个软件"

方案B（大一统单体）：所有功能在一个窗口，标签页切换
  优点：开发简单，体验统一
  缺点：桌宠无法悬浮桌面；越做越臃肿

方案C（壳+插件）：轻量主程序壳 + 各功能作为独立插件，可嵌入可弹出
  优点：整体感+模块独立性兼得；桌宠可弹出桌面；后续加功能不伤筋骨
  缺点：前期搭框架多花约一周
```

选择 C 的理由：**桌宠天生需要独立窗口**（必须能从主窗口弹出，A 能做到但体验割裂，B 做不到）、**DIY 是核心竞争力**（以后大概率有新想法，插件架构加功能不用重写）、**上次崩盘就是因为架构缺乏约束**（插件系统本身就是约束，逼着每个模块按规矩来）。

程序员 5 月 15 日确认选择方案 C。

---

### 阶段 2：设计深化（5 月 15-16 日）—— 把所有"待定"变成"已定"

**5 月 15 日：UI 设计讨论**

确定了产品的核心设计理念：**角色和窗口分离但关联——关闭窗口角色不消失，退回桌面。角色始终在线，是一个"伴侣"而非软件装饰。**

三级结构：
```
Level 1 桌宠模式 — 角色悬浮桌面，无窗口，自由走动/待机
Level 2 聊天模式 — QQ 面板式：左侧竖排头像栏 + 右侧聊天区，角色骑在顶栏边框
Level 3 设置模式 — 独立弹窗浮在聊天窗口上方，左侧分类导航 + 右侧内容区
```

**每个 Level 之间的过渡**是设计的核心难点：
- 双击桌宠 → 聊天窗口打开，角色跳上边框（300ms ease-out）
- 关闭窗口 → 角色跳下回到桌面（250ms ease-out）
- 从窗口拖拽角色到桌面 → 自由态
- 从桌面拖拽角色到窗口 → 坐回边框
- 最大化窗口 → 角色跳下桌面（最大化没地方坐）
- 恢复窗口 → 角色跳回

**5 月 16 日：设计系统终审**

定义了完整的设计 token：5 套主题色 × 2 种明暗模式 = 10 套配色，每套精确到色值（如暖桃暗色：主背景 `#1E1B1A`、AI 气泡 `#2A2625`、用户气泡 `#3D2824`、强调色 `#F0A090`）。

所有间距、圆角、阴影、动效参数都写死在 `design-system.md` 中，**不是"大概"而是"精确到像素"**。这是 Code as Design 的前提——如果设计师说"这里间距大概 16px 左右"，AI 写出来的代码就是各个模块各不相同。

**这一天同时处理了 8 个 UI 边界情况**：

| 难点 | 决策 |
|------|------|
| Level 2→3 过渡方式 | 独立弹窗浮在聊天上方（不是替换内容区也不是独立窗口） |
| 单角色时左侧栏闲置 | 多角色 52px / 单角色缩至 22px（迷你头像+"+"号） |
| TTS 播放缺视觉反馈 | 气泡声波指示器 + 角色"说话中"状态 |
| 空状态——初始启动无角色 | 4 个官方角色横排卡片选择 + 引导模式（角色弹 3-5 句引导气泡） |
| AI 请求中/失败 | 角色优先用自身表情反馈（思考/委屈/捂嘴），关键错误（API 失效）弹出设置 |
| 桌宠生命周期 | 关闭/最小化→跳下桌面，最大化→跳下，恢复→坐回，拖窗→跟着走 |
| 右键对话气泡 | 漫画风，鼠标移出消失，连续右键堆叠飘动，朝向跟随屏幕位置 |
| 系统托盘 | 当前角色大头照图标，右键菜单含显示/隐藏桌宠、切换角色、退出 |

**为什么这很重要**：AI 在写 UI 代码时遇到这些边界情况会自己"猜"一个实现。如果有 8 个边界情况，AI 就猜 8 次，程序员也可能猜 8 次——结果就是 UI 行为不一致。**在设计阶段一次性全部决定，代码就是执行，不是探索。**

---

### 阶段 3：技术决策（5 月 17 日）—— TTS 方案选型的曲折

这是整个项目中技术决策最典型的案例。

**初始推荐被推翻的过程**：

```
Round 1: Claude 分析 5 个方案 → 推荐 Edge-TTS（免费、中文好、20+音色、难度 2/5）
         ↓
Round 2: 用户试听 Edge-TTS → 音质不行，毙掉
         ↓
Round 3: 考虑讯飞云 API（音质顶尖、难度 2/5、~¥50-200/月）
         ↓
Round 4: 发现需要注册认证获取 Key → 违背"零用户门槛"原则，毙掉
         ↓
Round 5: 最终确定 IndexTTS（B 站开源、零样本语音克隆、音质 MOS 4.01、完全离线）
         代价：模型 ~2.3GB、需要 NVIDIA 显卡、Python 环境需打包进 Electron
```

**最终方案的代价清单**（每个代价都有应对）：
- 安装包增大 ~3-5GB → 提醒用户；考虑模型延迟下载
- 需要 NVIDIA 显卡（GTX 1660 6G 最低） → 启动时检测 GPU，无显卡时灰掉 TTS + 提示
- Python 子进程管理 → Electron 主进程启动 Python HTTP 服务，通过 localhost 通信
- 4 个角色不同音色 → 每个角色 3-5 秒参考音频，IndexTTS 零样本克隆

**这个决策过程的价值**：不是 Claude 推荐什么就定什么，而是**每一轮都带着约束条件（零门槛、中文、免费）去检验**。两轮毙掉方案后才找到 IndexTTS。如果没有"零用户门槛"这个写死的约束，可能就选了讯飞——表面简单，但把用户挡在注册认证门外。

---

### 阶段 4：规范化（5 月 18 日）—— 建立脚手架和素材管线

**技术侧**：程序员搭建 Electron 项目脚手架，确认插件框架采用简单模块加载（ES module `import()` 动态加载，共享渲染进程）。

**美术侧**：Claude 出具素材制作清单，精确到每个角色的每个动画的帧数：

```
13 个动画 / 角色，~80 帧（捷径后 ~38 帧）
  ├── P0 必须（7 个）：idle / walk / blink / talk / grabbed / jump_on / jump_off
  ├── P1 交互（3 个）：react_happy / react_shy / react_angry
  └── P2 错误（3 个）：think / sad / silent
```

同时设计了**分层画法**（身体层 + 表情层分离）来省帧数，以及**捷径方案**（walk 不画、只画一套向右用 CSS flip 翻转、错误表情简化成静态）。

**为什么这很重要**：美术和编程的交接是最容易出问题的环节。没有这个清单，程序员不知道需要哪些素材、什么格式、什么规格；美术不知道优先级、不知道捷径是什么。**把隐性的交接需求变成显性的清单。**

---

### 阶段 5：开发（5 月 27 日 - 6 月 2 日）—— 快速推进

```
5/27  用户制作 Midjourney 表情差分 prompt 模板
6/1   程序员提交：完整桌宠交互系统 + 聊天 UI + 设置窗口
6/2   程序员提交：重构角色系统 + 桌宠形象上传 + 设置界面完善
      用户提交：桌宠动作视频素材
```

这一阶段的特点是**快**——3 天堆出了桌宠、聊天、设置三大模块。但快有快的代价。

---

### 阶段 6：Bug 审查（6 月 3 日）—— 发现问题严重到可以宣告"上一版白做"

代码审查发现了 **15 项问题（6 个 Bug + 9 个功能缺失）**，其中 3 个致命：

**致命 Bug 1：AI 对话是假的**
所有回复从 6 条固定文案随机选，完全不调用 API。设置中的 API 地址/Key/模型字段在代码中完全未被引用。产品核心功能不存在。

**致命 Bug 2：设置 5 个字段不保存**
性格描述、语音 ID、API 地址、API Key、模型选择——全部未绑定 `value`/`onChange`，用户输入即丢弃。

**致命 Bug 3：源码与构建产物脱节**
`src/` 目录只有 React.StrictMode 空壳显示"开发中"，全部实际逻辑仅存在于 ASAR 构建产物中。这意味着**无法基于源码修 bug**——每次改完需要重新从 ASAR 反向提取。

**为什么会这样**：程序员使用 AI 快速堆出能跑的原型，但 AI 在每次独立对话中没有架构约束——它不知道 API 必须真实调用、不知道表单必须持久化、不知道源码必须可维护。**这正是"宪法优先"要防止的问题，而这次因为宪法没有被强制执行，问题重现了。**

---

### 阶段 7：风险评估（6 月 5 日）—— 把 Bug 转化为可管理的风险

从 Bug 清单中提炼出 **12 项风险**，按严重程度分四级，并建立了**阻塞关系图**：

```
🔴 致命（3 项）：假对话、设置不存、代码连贯性崩溃
🟠 高（4 项）：素材进度阻塞 P0-P2、无首次启动引导、无主题系统、桌宠挡住鼠标
🟡 中（3 项）：退出不保存、源码与构建脱节、打包体积 5GB+
🟢 低（2 项）：集成显卡不支持 TTS、上传图片无处理
```

**阻塞关系图**是这次风险评估的核心创新：

```
素材制作 (R4) ──阻塞──▶ P0 桌宠框架
                    ──阻塞──▶ P1 动画交互
                    ──阻塞──▶ P2 完整动画状态机

源码脱节 (R9) ──阻塞──▶ 所有 Bug 修复
假对话 (R2)  ──阻塞──▶ P1 聊天核心
设置不存 (R3) ──阻塞──▶ P1 API 接入 / P3 设置+DIY
```

这个图的价值：团队一看就知道**先修什么**。R2 + R3 + R9 不解决，后续所有开发都无意义——它们阻塞了所有下游工作。

---

## 三、遇到的每个问题及其解决方案

### 问题 1：跨角色语言不通 → "翻译层"

**场景**：
- 用户说"桌宠要可爱，偶尔眨眼"→ 程序员不知道要建几个动画状态、怎么触发
- 程序员说"用 Electron 打包体积大"→ 用户不知道对自己有什么影响

**不是"翻译"语言，而是转换为可执行的结构**：

| 用户说 | Claude 翻译给程序员 |
|--------|-------------------|
| "桌宠要可爱，偶尔眨眼" | 帧动画组件，2 个动画状态(idle/blink)，blink 叠加在 idle 上，随机定时器触发，间隔 2-5s |
| "DIY 就是用户能上传角色图调性格" | 上传接口 + 图片裁剪（Canvas crop）+ prompt 模板动态注入，性格用标签+自由文本双模式 |
| "TTS 声音要自然" | TTS 引擎选型 5 方案对比，流式播放接口要求，角色 talk 动画同步 |

反过来也翻译：

| 程序员说 | Claude 翻译给用户 |
|---------|----------|
| "用 Electron 打包体积大" | "安装包约 150MB+，加上 TTS 模型后约 5GB，功能不受影响" |
| "这个需要 60fps 渲染管线" | "可以做，加载会多 1 秒，能接受吗？" |
| "IPC 同步有延迟" | "角色跟随窗口移动时可能有轻微拖影" |

**通用化**：任何跨领域协作（产品×开发、设计×工程、业务×技术）都需要一个翻译角色——TA 不替代任何一方，而是确保信息在传递中不衰减、不被各自的行话扭曲。

---

### 问题 2：AI 代码"局部最优，全局不兼容" → "宪法优先"

**场景（上一版崩溃的根因）**：程序员用 AI 写了聊天模块、桌宠模块、设置模块，每个单独看都没问题。拼在一起发现——聊天消息格式和桌宠动画系统用的角色 ID 字段名不一样、设置保存的数据结构和聊天读取的数据结构不一样、CSS 色值各模块自己硬编码互不一致。

**解决方案：在写任何代码前，先建一套书面"宪法"**：

```
architecture.md    → 技术栈、模块拆分、数据流方向、插件加载方式
interfaces.md      → 每个模块的输入/输出接口（函数签名、IPC 通道名、数据结构）
design-system.md   → 所有 CSS 变量、间距、圆角、阴影、动效参数（精确到像素）
ui-spec.md         → 三级菜单结构、交互逻辑、状态转换规则
```

每次 AI 写代码前**必须先读这些文件**。不是"建议"，是硬约束。

**实际效果**：制定宪法后开发的 v0.1.0 仍然崩溃了——但崩溃原因恰恰是**没有严格执行宪法**。Bug 审查发现的"源码与构建脱节"就是典型症状——AI 在 ASAR 产物里写了一堆逻辑，但没有回迁到 src/，因为宪法规定了 src/ 是唯一真相源但实际编码时没有被 enforcement。

**教训**：宪法不只要写，还要强制执行。解决方案是 Claude 每次编码前强制 `Read` 对应文档，并在代码评审时检查是否符合宪法。

**通用化**：把隐性约束显性化。无论用什么 AI 工具、搭什么项目，先花时间把"宪法"写清楚——后续效率提升远超前期投入。宪法的形式不重要（可以是 markdown、Notion、代码注释），重要的是**它存在、它被读取、它被遵守**。

---

### 问题 3：非技术用户无法验收程序员工作 → "Spec 作为契约"

**场景**：程序员说"做完了"，用户看不懂代码只能信。等用起来发现不对已经晚了。

**解决方案：五步闭环，spec 是中间契约**：

```
① 用户用自然语言描述"我想要 XXX"
   ↓
② Claude 翻译成 spec —— 功能规格书，程序员能直接对着编码
   ↓
③ 程序员审阅 spec，提出技术约束或修改建议
   ↓
④ 程序员 + Claude 按照 spec 编码（不是按用户原话编码）
   ↓
⑤ 用户对着 spec 验收效果，而不是看代码
```

**spec 的价值**：
- 用户不看代码，只看"spec 里写的功能是不是都能用"
- 程序员不看用户的模糊描述，只看 spec 里的技术定义
- 如果有分歧，回看 spec

**实际案例**：桌宠生命周期 spec 写了 11 种窗口操作对应的角色行为，每种行为有动画名称和时长。程序员实现时就对着这张表写，用户验收时就对着这张表试——关闭窗口角色跳下了吗？最大化时角色跳下了吗？

**通用化**：需求可验证性。所有模糊需求必须在上游转化为可逐项检查的清单。让非技术方能"对着打勾"。

---

### 问题 4：技术选型对非技术人员是黑盒 → "多维决策表"

**场景**：用户说"TTS 声音要自然"。程序员有 5 个可选方案，每个都有不同的代价。用户不知道怎么做选择。

**解决方案：把每个方案拆成非技术人员也能看懂的维度**：

| 方案 | 难度 | 成本 | 延迟 | 一句话优劣 |
|------|------|------|------|-----------|
| 浏览器 SpeechSynthesis | 1/5 | ¥0 | 即时 | 音质像机器人 |
| Edge-TTS | 2/5 | ¥0 | <1s | 音质好但依赖微软外部接口 |
| VITS 本地模型 | 4/5 | 电费 | <2s | 需要 GPU，安装复杂 |
| GPT-SoVITS | 4/5 | 电费 | <3s | 可定制角色音色但门槛高 |
| 阿里云/讯飞 API | 2/5 | ¥50-200/月 | <0.5s | 稳定音质好但需要注册账号 Key |

**迭代决策过程**：
1. 先毙掉浏览器 TTS（音质不达标）
2. 推荐 Edge-TTS → 用户试听 → 音质不行，毙掉
3. 考虑讯飞 → 需要注册认证 → 违背"零用户门槛"原则，毙掉
4. 最终定 IndexTTS → 虽增加 2.3GB 安装体积但换取零门槛+零成本+音质天花板

**关键洞察**：用户不是需要理解技术细节，而是需要理解**每个选择的代价**。难度 4/5 vs 2/5、¥0 vs ¥50/月、"需要注册"vs"零门槛"——这些维度让非技术方能参与决策。

**通用化**：技术评审的核心不是"哪个最好"，而是**把取舍讲清楚让非技术人员也能参与决策**。

---

### 问题 5：文档写完两周就腐烂 → "活文档 + 自动同步"

**场景**：所有项目都要求写文档，但写完两周后就过时了。维护文档是额外负担，没人做。

**解决方案：让文档自己活**。

配置 Claude Code 钩子实现自动同步：

| 时机 | 自动动作 |
|------|---------|
| Claude 编辑/写入文件后 | `git add → commit → push` |
| Claude 对话开始前 | `git pull`（拉取最新） |

**效果**：
- 30 次提交记录了从规划到开发到 bug 审查到风险评估的全过程
- 每一条 `git log` 都是可追溯的决策时间线
- 改错了可以 `git revert` 回退
- 两台电脑之间实时同步

**配套的 Git 配置**（多机互通）：
```
GitHub/Gitee 私有仓库 + credential.helper store 免密
每次 Edit/Write 后自动 commit → push
每次 SessionStart 自动 pull
```

**通用化**：文档腐烂的根因是"维护文档需要额外动作"。把更新嵌入到工作流中自动化——用 Git hooks、CI、或 AI 钩子。文档不再是"写一次就死的产物"，而是"每次讨论都在生长的东西"。

---

### 问题 6：流程本身会过时 → "流程自更新"

**场景**：定好的流程用两周就不适用了，但"改流程"需要开会讨论、审批、同步全员——太重了，一拖就死了。

**解决方案：`workflow.md` 本身在 Git 里版本控制。**

任何一方觉得"这步不对"→ 直接告诉 Claude → Claude 更新 `workflow.md` → 自动 commit + push → 下周按新规则执行。

**不需要开会，不需要审批**。流程和代码一样：可迭代、可版本化、可回滚。

**通用化**：流程本身应该是产品的一部分。当"改进流程"的成本无限趋近于零，流程就会越来越贴合实际工作。

---

### 问题 7：进度跟踪靠自觉 → "从文档状态自动生成周报"

**场景**：到了周日谁也想不起来这周干了什么。写周报靠回忆和编造。

**解决方案：周报不靠"写"，靠"收"。**

Claude 从 docs/ 中各 spec 的状态自动生成周报，固定格式：

```markdown
## 本周完成
- [功能A] 已完成，验收通过
## 本周未完成 / 阻塞
- [功能C] 原因
## 下周计划
- 程序员：XXX | 用户：XXX | Claude：XXX
## 风险提示
## 决策记录
```

周报内容来源于文档状态（spec 里的"✅ 已确定 / ⬜ 待开发"），不是凭空编写。

**通用化**：日报周报不应该靠人"写"，应该靠系统从工作过程中**自动采集**。

---

### 问题 8：多台电脑协作数据不同步 → "Git 仓库作为协作中枢"

**场景**：用户和程序员各一台电脑，都用 Claude Code，但产出文件（docs、specs、素材清单）在不同机器上各自修改，版本混乱。

**解决方案**：
- 项目工作流文件夹本身就是 Git 仓库
- Claude Code 钩子自动在 Edit/Write 后 push，启动前 pull
- 两台电脑始终看到对方的更新

**通用化**：多人 + 多 AI 会话协作时，Git 是最简单的共享真相源。不是代码才该用版本控制——文档、设计 spec、素材清单都应该版本化。

---

### 问题 9：桌宠素材工作量大且阻塞所有开发 → "捷径方案 + 分层画法"

**场景**：13 个动画 × 4 个角色 = 520 个文件。如果等全部素材做完再写桌宠代码，整个 P0-P2 全部阻塞。

**解决方案**：
1. **捷径方案**：walk 不画（瞬移）、只画一套用 CSS flip 翻转、错误表情简化成 1-2 帧静态
2. **分层画法**：身体层（idle）+ 表情层（blink/talk）分离——blink 只画眼睛 4 帧，不用重画整个身体
3. **逐角色递进**：先做小桃一个角色跑通流程 → 验证效果 → 复制模板做其余三个

结果：520 文件 → 捷径后 ~164 文件 → 先做一个角色只需 ~38 帧。

**通用化**：阻塞项不要一次性全部解决。先找一个捷径让流程跑通，再逐步补全。

---

### 问题 10：Bug 太多不知道先修哪个 → "风险分级 + 阻塞关系图"

**场景**：审查发现 15 项问题，严重度不同，修的顺序混乱。

**解决方案**：
1. 四级严重度标记（致命/高/中/低）
2. **阻塞关系图**——画出哪些风险阻塞了哪些开发阶段：

```
源码脱节  ──阻塞──▶ 所有 Bug 修复
假对话    ──阻塞──▶ P1 聊天核心
设置不存  ──阻塞──▶ P1 API 接入 / P3 设置+DIY
素材进度  ──阻塞──▶ P0 桌宠框架 → P1 动画交互 → P2 完整动画状态机
```

**一图胜千言**：任何团队成员一看就知道"现在最重要的是修 R2+R3+R9，其他的之后再管"。

**通用化**：Bug tracker 列出来的是清单，阻塞关系图画出来的是**依赖拓扑**。有了拓扑才知道真正的瓶颈在哪。

---

## 四、治理框架：可迁移到任何项目的结构

### 目录结构

```
project/
├── docs/              ← 宪法层（写代码前 AI 必读）
│   ├── vision.md      ← 产品愿景：一句话 + 目标用户 + 竞品差异
│   ├── features.md    ← 功能清单 + 子功能拆解 + 原则声明
│   ├── architecture.md← 技术栈、模块拆分、数据流
│   ├── interfaces.md  ← 模块间接口契约（函数签名、IPC 通道、数据结构）
│   ├── design-system.md← 所有视觉 token：色值/间距/圆角/阴影/动效（精确到像素）
│   ├── ui-spec.md     ← 交互逻辑：三级结构、状态转换、边界情况决策
│   └── workflow.md    ← 流程规则（可随时自改）
├── specs/             ← 决策记录层（每个功能的技术方案分析）
│   ├── architecture-analysis.md  ← 3 方案对比 + 推荐 + 决策
│   ├── tts-analysis.md           ← 5 方案多轮筛选，每轮毙掉原因
│   ├── ui-implementation.md      ← 窗口架构/主题系统/组件树/IPC/动画状态机
│   ├── development-plan.md       ← P0-P5 分阶段计划 + 验收标准
│   ├── animation-assets.md       ← 13 动画规格 + 捷径 + 分层画法
│   ├── asset-checklist.md        ← 素材制作清单（逐项打勾）
│   └── risks.md                  ← 12 项风险分级 + 阻塞关系图
├── bug/               ← 质量记录（Bug 清单 + 录屏证据）
├── reports/           ← 自动化产出（周报，从文档状态生成）
└── .claude/           ← 自动化引擎（Git 自动同步钩子）
```

### 四条铁律

1. **宪法优先**：写代码前必须读 docs/。AI 编码也不例外。
2. **决策留痕**：每一个技术选择都记录在 specs/ 中，包含"毙掉的方案和毙掉的原因"。
3. **流程自改**：谁痛谁改 workflow.md，不需要开会。
4. **自动化同步**：每次写文件自动 commit+push，每次对话自动 pull。

### 回答三个根本问题

| 问题 | 解法 | 核心机制 |
|------|------|---------|
| **信息怎么不丢？** | 翻译层 + 宪法文档 | 用户话→spec、程序员话→用户话 |
| **决策怎么做？** | 多维分析表 + spec 契约 | 非技术人员看维度、技术人员看数据 |
| **过程怎么不腐？** | 自动化同步 + 自更新流程 | Git hooks + workflow.md |

---

## 五、当前状态与下一步

### 项目状态（2026-06-06）

| 维度 | 状态 |
|------|------|
| 产品设计 | ✅ vision / features / UI / 设计系统全部确定 |
| 技术选型 | ✅ 架构 C / Electron / IndexTTS / CSS 变量主题 |
| 代码开发 | ⚠️ v0.1.0 有 3 个致命 Bug（假对话/设置不存/源码脱节） |
| 素材制作 | ⬜ 4 角色 × 3 静态图 × 13 动画 = 0% 完成 |
| 风险评估 | ✅ 12 项已分级，阻塞关系已映射 |

### 首要行动

```
R2 假对话 ──→ 重写 sendMessage，对接真实 API
R3 设置不存 ──→ 绑定 value/onChange，实现本地持久化
R9 源码脱节 ──→ 代码从 ASAR 回迁到 src/，以 src 为唯一真相源
```

这三个不解决，后面的开发没有意义。

### 这个工作流的本质

它不是什么"项目管理方法论"，而是一套**用 AI 放大人类协作能力**的操作系统。核心假设是：**AI 让编码变快了，但让"保持一致性"变难了。** 所以工作流的核心不是让 AI 更快地写代码，而是**确保 AI 写出来的代码不会互相打架**。

这套框架的具体形式（用 markdown 还是 Notion、用 Git 还是飞书、用 Claude 还是其他 AI）不重要，重要的是**识别你协作中的瓶颈属于上述哪一类，然后针对性地设计解法**。

---

> 本文件随项目演进持续更新。最后更新：2026-06-06。
